const axios = require("axios");
export function viewPlaylists(req, res) {
    console.log('inside');
    // userInfo = await axios.get("/auth/getuser")
    // .then(resp => {
    //     console.log(resp);
    // })
    // .catch(err => {
    //     console.log(err);
    // })

    // console.log(userInfo);
    // // res.redirect("/")
  
}
// export default viewPlaylists;